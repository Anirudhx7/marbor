# Used by goreleaser: copies the prebuilt binary instead of building from source.
# mesh.db is mounted/persisted at runtime, not baked into the image.
FROM alpine:latest
RUN apk add --no-cache ca-certificates
WORKDIR /root
COPY ollama-mesh .
EXPOSE 11434 8080 9090
ENTRYPOINT ["./ollama-mesh"]
